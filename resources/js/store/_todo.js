import axios from "axios";
export default {
    state: {
        user: null,
    },
    mutations: {
        // example(state, item) {},
    },

    actions: {
        // example({commit, state, dispatch, getters}, {props}),
    },
    getters: {
        // example(state) {}
    }
};